# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.ruff_cache/
.pybuild/

# Packaging build output (see packaging/README.md -- these are only ever
# meant to be produced by packaging/build-{deb,rpm}.sh, inside a
# container targeting the real distro, never committed)
/debian/
dist-packages/
*.deb
*.deb.sha512
*.rpm
*.rpm.sha512

# Local test/example scratch data (e.g. storage roots created by running
# examples or the CLI by hand)
example-strings/
example-stories/
rmstory-stories/
*.sqlite3
*.db

# Editors / OS / local tooling
.DS_Store
.vscode/
.idea/
*.swp

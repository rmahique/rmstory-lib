# Packaging build image for rmstory-debian-bookworm. Bakes in every
# dependency packaging/build-deb.sh needs, on the same distro the package
# targets -- generated from (and must be kept in sync with) the combo
# definition in ../../../.github/workflows/build-packages.yml.
FROM debian:bookworm

RUN apt-get update && \
    apt-get install -y build-essential debhelper dh-python python3-all python3-setuptools python3-yaml python3-pytest python3-build pybuild-plugin-pyproject tar git && \
    rm -rf /var/lib/apt/lists/*
